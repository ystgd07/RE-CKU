import { db } from "./index.schema";

//   const newUser = db.query(`INSERT INTO user(username,email,phoneNumber,password) VALUES(?,?,?,?)`, [

// export const create = async(data:Record<string , string|number>)=>{
    
//     await db.query(`INSERT INTO board(title,content, hashTags,fromUserId,hasResumeId) VALUES(?,?,?,?,?)`,[])
// }